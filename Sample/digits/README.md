```
The translation in the `digits.ocs` file equates to a list of numbers from 1 to 5000.

Number zero is not included, because its helpful to see the file line alongside the encoded
oceanscript. Zero would be `_>...`.
```